import { Header as MHeader, Text } from "@mantine/core";

function Header() {
    return (
        <MHeader height={80} p="md">
            <Text>Header</Text>
        </MHeader>
    );
}

export default Header;
